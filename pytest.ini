[pytest]
# Stable pytest configuration for Niblit cloud server.
#
# pythonpath ensures "app" and "tools" packages are importable during
# pytest collection regardless of working directory, CI runner, Termux,
# or local-runtime environment.  This eliminates "ModuleNotFoundError:
# No module named 'app'" collection failures.
pythonpath = .
testpaths = tests
